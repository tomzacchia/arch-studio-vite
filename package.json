{
  "name": "arch-studio-vite",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "APP_ENV=development vite --open",
    "build": "APP_ENV=production vite build",
    "build:local": "vite build",
    "preview": "vite preview --open --host"
  },
  "devDependencies": {
    "sass": "^1.49.7",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "@glidejs/glide": "^3.5.2",
    "dotenv": "^16.0.0"
  }
}
